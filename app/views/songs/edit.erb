<h1>Edit Song</h1>

You are editing <%= @song.name %>.

Enter new name for song:
<form action="/songs/<%= @song.slug %>" method="post">
  <input id="hidden" type="hidden" name="_method" value="PATCH">
  <input type="text" name="song[name]" value="<%= @song.name %>">
  <br><br>


<label>Add or remove genres for song:</label>
<br>
  <%all_genres.each do |genre|%>
    <label><%=genre.name%>
      <input id="<% genre.name %>" type="checkbox" name="song[genre_ids][]" value="<%=genre.id%>" <%='checked' if @song.genres.include?(genre) %>></input>
    </label>
    <br>
  <%end%>
  <br>

<label> And/or create genre to associate with this song:</label>
<br>
  <input type="text" name="genre[name]">
<br><br>

<label>Change song's artist:</label>
<br>
  <%all_artists.each do |artist|%>
    <input id="<%= artist.name %>" type="radio" name="song[artist_id]" value="<%=artist.id%>" <%='checked' if @song.artist_id == artist.id %>><%=artist.name%></input>
    <br>
  <%end%>
<br>
<label> Or create new artist to associate with this song:</label>
<br>
  <input id="Artist Name" type="text" name="artist[name]">
<br>
<input type="submit" value="Save">
</form>
