<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>
<h1>ALL SONGS</h1>
<ul>
<% all_songs.each do |song|%>
  <li>
    <a href="/songs/<%= song.slug %>" > <%= song.name  %></a>
  </li>
  <% end %>
</ul>
